# generated by: yake code goreleaser
version: 2
before:
  hooks:
    - go mod tidy
builds:
  - id: apt-transport-github
    binary: github
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
nfpms:
  - id: apt-transport-github
    package_name: apt-transport-github
    ids:
      - apt-transport-github
    bindir: /usr/lib/apt/methods
    maintainer: vitalvas
    vendor: vitalvas
    homepage: https://github.com/vitalvas/apt-transport-github
    description: APT transport method for GitHub releases
    license: MIT
    formats:
      - deb
    dependencies:
      - gnupg
    contents:
      - src: misc/apt-transport-github.sources
        dst: /etc/apt/sources.list.d/apt-transport-github.sources
        type: config
    scripts:
      postinstall: misc/postinstall.sh
checksum:
  name_template: checksums.txt
  algorithm: sha256
snapshot:
  version_template: '{{ .Version }}-{{ .CommitTimestamp }}-{{ .ShortCommit }}'
changelog:
  disable: true
release:
  github:
    owner: vitalvas
    name: apt-transport-github
  draft: false
  prerelease: auto
  mode: keep-existing
